// #include<stdio.h>
// #include<math.h>

// int main() {
   // float n, sr;
    //printf("write a number: ");
    //scanf("%f",&n);
   // sr = sqrt(n);
   // printf("the square root of %.2f is %.2f\n", n, sr);
   // return 0;
 // } 

#include<stdio.h>
#include<math.h>

int main() {
    float n, sr;
    printf("write a number: ");
    scanf("%f",&n);
    sr = sqrt(n);
    printf("the square root of %.2f is %.2f\n", n, sr);
    return 0;
}